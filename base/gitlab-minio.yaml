apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gitlab-tls-cert-minio
  namespace: gitlab
spec:
  dnsNames:
  - minio.openfellas.com
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  secretName: gitlab-tls-cert-minio